export class Column {
    constructor(title, fields) {
        this.title = title
        this.fields = fields
    }
}
export class Title {
    constructor(code, number, before, after) {
        this.code = code
        this.number = number
        this.before = before
        this.after = after
    }
}